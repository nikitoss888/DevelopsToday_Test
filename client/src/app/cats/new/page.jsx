import NewCat from "./New";

export const metadata = {
    title: "Create New Spy Cat Agent",
    description: "Create a new Spy Cat Agent",
};

export default function NewCatPage() {
    return <NewCat />;
}